{
  "name": "paroquia-manager-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src run dev",
    "build": "npm --prefix src install && npm --prefix src run build",
    "start": "npm --prefix src run start",
    "install:src": "npm --prefix src install",
    "postinstall": "npm run install:src"
  }
}
